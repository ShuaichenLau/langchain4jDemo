<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuan.mapper.AppointmentMapper">

    <select id="getOneByUsernameAndIdCard" resultType="com.yuan.entity.Appointment">
        select
         id,
         username,
         id_card as idCard,
         department,
         date,
         time,
         doctor_name as doctorName
        from appointment where username = #{username} and id_card = #{idCard}
    </select>

</mapper>